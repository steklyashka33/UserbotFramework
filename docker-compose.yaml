version: '3.8'

services:
  userbot-framework:
    build: .
    container_name: userbot-framework
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./sessions:/app/sessions
    ports:
      - "${MANAGER_API_PORT:-8000}:8000"
      - "${BOT_WEBHOOK_PORT:-8001}:8001"
